# 🚂 Railway Деплой TourVisor MCP Server

## 📋 Подготовка к деплою

### 1. Файлы для Railway
- `start.sh` - скрипт запуска
- `railway.toml` - конфигурация Railway
- `Dockerfile` - Docker конфигурация
- `requirements_mcp.txt` - зависимости

### 2. Переменные окружения в Railway
Добавь в Railway Settings → Variables:
```
PORT=8080
PYTHONUNBUFFERED=1
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
```

## 🔧 Деплой на Railway

### Вариант A: Через GitHub (рекомендуется)
1. Запушить изменения в GitHub
2. В Railway: New Project → Deploy from GitHub repo
3. Выбрать репозиторий `wagon3667-cloud/tourmcp`
4. Railway автоматически определит Dockerfile

### Вариант B: Прямой деплой
1. Установить Railway CLI: `npm install -g @railway/cli`
2. Логин: `railway login`
3. Деплой: `railway up`

## 🛠️ Отладка на Railway

### Проверка логов
```bash
railway logs
```

### Если ошибка с браузерами:
Добавь в railway.toml:
```toml
[deploy]
startCommand = "playwright install chromium && python3 http_server.py"
```

### Если проблемы с памятью:
В Railway Settings → Variables:
```
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=0
```

## 🌐 Доступ к API
После деплоя API будет доступен по адресу:
`https://your-app-name.railway.app`

### Эндпоинты:
- `GET /health` - проверка здоровья
- `POST /search_tours` - поиск туров
- `POST /quick_search` - быстрый поиск
- `GET /stats` - статистика

## 📊 Пример запроса
```bash
curl -X POST https://your-app.railway.app/quick_search \
  -H "Content-Type: application/json" \
  -d '{"query": "Дубай из Москвы на 5 ночей 5 звезд"}'
```

## ⚠️ Особенности Railway
- **Бесплатный тариф**: 500 часов/месяц, 512MB RAM
- **Ограничения**: Playwright может требовать больше памяти
- **Решение**: Использовать headless режим и оптимизировать

## 🔧 Оптимизация для Railway
Если не хватает памяти, можно:
1. Использовать более легкий браузер
2. Уменьшить количество concurrent requests
3. Добавить кэширование результатов
