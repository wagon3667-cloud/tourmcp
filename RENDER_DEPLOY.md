# 🎨 Render.com Деплой TourVisor MCP Server

## 🚀 Render.com - лучший выбор для Playwright

### Преимущества перед Railway:
- ✅ **768MB RAM** на бесплатном тарифе (vs 512MB на Railway)
- ✅ **Лучшая поддержка Playwright и браузеров**
- ✅ **Стабильная работа с headless Chrome**
- ✅ **Автоматические health checks**
- ✅ **SSL сертификаты из коробки**

## 📋 Деплой на Render

### 1. Подключение GitHub репозитория
1. Перейди на https://render.com
2. New → Web Service
3. Connect GitHub → выбери `wagon3667-cloud/tourmcp`
4. Render автоматически определит Python/Docker

### 2. Настройки Web Service
```
Name: tourvisor-api
Environment: Docker
Branch: main
Root Directory: (пусто)
Instance Type: Free (768MB RAM)
```

### 3. Переменные окружения (Environment Variables)
Добавь в Render Dashboard → Environment:
```
PORT=8080
PYTHONUNBUFFERED=1
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=0
```

### 4. Health Check
```
Health Check Path: /health
Auto-Deploy: Yes (для автоматического обновления)
```

## 🐳 Docker конфигурация для Render

Используется исправленный `Dockerfile` с правильными зависимостями:
- Ручная установка системных библиотек
- Playwright Chromium
- Правильные переменные окружения

## 🌐 Доступ к API

После деплоя API будет доступен по адресу:
`https://tourvisor-api.onrender.com`

### Эндпоинты:
- `GET /health` - проверка здоровья
- `POST /search_tours` - поиск туров
- `POST /quick_search` - быстрый поиск
- `GET /stats` - статистика
- `GET /get_countries` - страны
- `GET /get_departures` - города вылета

## 📊 Пример использования

```bash
# Проверка здоровья
curl https://tourvisor-api.onrender.com/health

# Поиск туров
curl -X POST https://tourvisor-api.onrender.com/quick_search \
  -H "Content-Type: application/json" \
  -d '{"query": "Дубай из Москвы на 5 ночей 5 звезд"}'
```

## ⚠️ Особенности Render

### Время загрузки (Cold Start)
- **Первый запрос:** ~30-60 секунд (запуск браузера)
- **Последующие запросы:** ~5-10 секунд
- **Решение:** Render Pro ($7/мес) для 24/7 работы

### Ограничения бесплатного тарифа:
- **750 часов/месяц** (хватает для 24/7)
- **768MB RAM** (достаточно для Playwright)
- **Спящий режим** после 15 минут неактивности

## 🔧 Оптимизация для Render

### Ускорение холодного старта:
1. **Кэширование браузеров** - уже в Dockerfile
2. **Оптимизированный запуск** - минимальные зависимости
3. **Health check** - поддерживает сервис активным

### Если не хватает памяти:
```bash
# В Dockerfile можно добавить:
ENV PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=1
```

## 📈 Мониторинг

Render предоставляет:
- **Logs** - логи приложения
- **Metrics** - метрики производительности
- **Health Checks** - статус сервиса
- **Auto-Deploy** - автоматические обновления

## 🎯 Готовность к продакшен

После деплоя твой API будет готов для:
- **Любых LLM** через HTTP запросы
- **Веб приложений** через CORS
- **Мобильных приложений** через REST API
- **Ботов** и ассистентов

**Render.com - идеальный хостинг для TourVisor MCP Server!** 🚀
