# 🚀 Деплой TourVisor MCP Server на GitHub

## 📋 Инструкции по развертыванию

### 1. Создание репозитория на GitHub

1. Перейди на https://github.com/new
2. Название репозитория: `tourvisor-mcp-server`
3. Описание: `MCP Server для поиска туров через TourVisor API с обходом блокировок`
4. Выбери "Public" или "Private"
5. НЕ ставь галочку "Add a README file" (у нас уже есть)
6. Нажми "Create repository"

### 2. Пуш кода на GitHub

После создания репозитория, выполни в терминале:

```bash
# Если еще не настроен remote
git remote set-url origin https://github.com/ТВОЙ_НИК/tourvisor-mcp-server.git

# Пуш с аутентификацией
git push -u origin main
```

### 3. Аутентификация GitHub

Если используется парольная аутентификация (устарела):

#### Вариант A: Personal Access Token
1. Перейди в GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создай новый токен с правами `repo`
3. Используй токен вместо пароля при пуше

#### Вариант B: SSH ключи
```bash
# Сгенерируй SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"

# Добавь ключ в GitHub
# Скопируй вывод команды:
cat ~/.ssh/id_ed25519.pub

# Добавь ключ в GitHub Settings → SSH and GPG keys
# Измени remote на SSH:
git remote set-url origin git@github.com:ТВОЙ_НИК/tourvisor-mcp-server.git
```

### 4. Структура репозитория

```
tourvisor-mcp-server/
├── fixed_departure_api.py    # Ядро API (обход TourVisor)
├── mcp_server.py            # MCP сервер
├── requirements_mcp.txt     # Зависимости
├── README_MCP.md           # Документация
├── DEPLOY_INSTRUCTIONS.md  # Этот файл
└── .gitignore              # Игнорируемые файлы
```

### 5. Установка для пользователей

```bash
# Клонирование репозитория
git clone https://github.com/ТВОЙ_НИК/tourvisor-mcp-server.git
cd tourvisor-mcp-server

# Установка зависимостей
pip install -r requirements_mcp.txt

# Установка браузеров Playwright
playwright install chromium

# Запуск MCP сервера
python3 mcp_server.py
```

### 6. Интеграция с Claude Desktop

Добавить в `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "tourvisor-api": {
      "command": "python3",
      "args": ["/путь/к/tourvisor-mcp-server/mcp_server.py"]
    }
  }
}
```

## 🎯 Файлы готовые к деплою

✅ **Закоммичено:**
- `fixed_departure_api.py` - основное API
- `mcp_server.py` - MCP сервер  
- `requirements_mcp.txt` - зависимости
- `README_MCP.md` - документация
- `.gitignore` - исключение лишних файлов

❌ **Не включено в репозиторий (игнорируется):**
- Все JSON файлы с результатами
- HTML файлы отладки
- Временные скрипты
- Виртуальное окружение
- Скриншоты ошибок

## 🚀 Следующие шаги

1. Создай репозиторий на GitHub
2. Запуши код с аутентификацией
3. Проверь что все файлы загрузились
4. Протестируй клонирование и установку
5. Опубликуй ссылку на репозиторий

---

**Готово!** MCP сервер готов к деплою на GitHub! 🎉
